{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && tailwindcss -i root.css -o dist/root.css --minify",
    "preview": "vite preview",
    "watch": "tailwindcss -i root.css -o public/root.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.3",
    "vite": "^4.4.5"
  }
}
